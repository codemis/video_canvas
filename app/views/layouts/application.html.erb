<!DOCTYPE html>
<html>
<head>
  <title>Videocanvas</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<ul>
		<li>
			<%= link_to "Home", root_path %>
		</li>
		<li>
			<%= link_to "Annotation", annotation_path %>
		</li>
		<li>
			<%= link_to "Logout", destroy_user_session_path, method: :delete if current_user && !current_user.guest_user %>
		</li>
		<li>
			<%= link_to "Logout, here for development", destroy_user_session_path, method: :delete if current_user %>
		</li>
	</ul>

	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>

<%= yield %>
	<%= yield :footer %>
</body>
</html>
